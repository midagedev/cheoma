// Outbound product links. One source so chrome, References and the social card
// cannot drift apart — the repo URL also appears in app/index.html meta tags.
export const REPO_URL = 'https://github.com/midagedev/cheoma';
export const SITE_URL = 'https://cheoma.midagedev.com';

// Shown as the visible label of a repo link (the bare host+path reads as a
// source address, which is what a viewer is looking for).
export const REPO_LABEL = 'github.com/midagedev/cheoma';
