# 한양 시전행랑 표현 계약

> - **상태**: 조사·구현 계약
> - **관련 이슈**: GitHub #128, #218 (줄 분절), #227 (표식 schema v2), #54 (벽체 완결 schema v3),
>   상위 폴리시 #75 / #209
> - **조사 기준일**: 2026-07-24 (분절 제품 해석 2026-07-27, 벽체 완결 2026-08-04)
> - **대상**: 한양 중심 간선변의 시전행랑
> - **비대상**: 특정 연도·발굴지·개별 시전의 실측 복원

이 문서는 시전행랑을 단순한 흰 상자 열에서 벗어나게 하되, 사료가 말하지 않는 점포 입면을
사실처럼 만들지 않기 위한 경계다. **확인된 사실**, **남아 있는 한계**, **cheoma의 경량 제품
해석**을 분리한다. 원문 문장·사진·도면·발굴 이미지는 저장소나 앱 번들에 복제하지 않는다.
사용자에게 공개할 선별 출처와 이용조건은 [`credits.md`](credits.md)의 40번 항목에도 같은 범위로
남긴다.

## 1. 출처가 확인하는 사실

### 1.1 가로를 따라 이어진 행랑과 칸

국사편찬위원회 우리역사넷 「조선 전기 신도의 시전 상업」은 1412년(태종 12)부터 세 차례의
공사로 한양의 행랑 2,027칸이 조성되었고, 운종가에서 종묘 앞 누문 사이와 종루에서 광통교
사이 등이 시전 전용 행랑으로 쓰였다고 설명한다. 2,027칸 전체가 모두 시전이었다는 뜻은
아니다.

서울역사박물관 「청진동 발굴 유물」은 청진지구에서 **종로를 따라 일렬로 늘어선 시전행랑**,
그 뒤의 피맛길, 주택가와 막다른 골목이 함께 확인되었다고 설명한다. 따라서 제품이 가져올
핵심 공간 언어는 독립 점포 상자의 반복이 아니라 다음과 같다.

- 간선에 평행한 긴 가로벽과 처마선
- 그 선을 나누어 읽게 하는 반복 칸과 기둥
- 전면 시전행랑 뒤에 이어지는 피맛길·배후 공간의 방향성

이 근거는 모든 시전이 한 동의 끊김 없는 지붕이었다거나, 각 칸의 실측 폭·깊이가 같았다는
뜻은 아니다. cheoma의 기존 점포 footprint와 pitch는 도시 배치·가독성을 위한 제품 수치로
남는다.

### 1.2 칸은 사용과 관리의 단위였다

같은 우리역사넷 자료는 초기 시전 상인이 행랑 **한 칸마다** 봄·가을 공랑세를 냈다고
기록한다. 한국사 연대기 「금난전권」은 한성부가 시전의 행랑 규모를 칸 수, 상인 이름,
수공업자와 함께 시안에 기록해 관리했다고 설명한다. 「도가와 시전 행랑」은 조선 후기에
낡거나 불탄 행랑을 민간 자본으로 다시 짓고 시전 상인에게 임대료를 받았다고 설명한다.

그러므로 칸별 반복을 영업 단위로 읽히게 하는 것은 근거가 있다. 다만 후기 임대료의 정확한
산정 단위·금액까지 칸별이었다고 단정하지 않는다. 제품에서는 하나의 계획 칸을 하나의
가벼운 점포 의미 단위로 사용하되, 역사적 세율이나 소유관계를 시뮬레이션하지 않는다.

### 1.3 표식과 물종 어휘

우리역사넷은 초기 시전행랑에 표를 세워 **시전의 이름과 판매 물종**을 표시했다고 설명한다.
한국사 연대기도 시명과 판매 물종이 기록되었다고 교차 확인한다. 이는 현대식 개인 상호,
대형 fascia 간판, 네온 광고의 근거가 아니다.

「유분각전과 무분각전」은 비단·무명·종이·모시·생선·곡물·유기·신발·철물 등 시전별
판매 물종의 넓은 어휘를 제공한다. 제품은 이 목록을 다음처럼 거친 실루엣 군으로만 축약한다.

| 제품 물종 군 | 가까이서 읽힐 최소 어휘 | 배제 |
| --- | --- | --- |
| 직물·종이 | 접거나 만 얇은 묶음 | 읽을 수 있는 현대 포장·인쇄 상표 |
| 곡물·마른 물건 | 낮은 포대·바구니 | 실제 식품 사진 텍스처 |
| 어물 | 절제된 건어물 묶음·바구니 | 젖은 생선의 사실적 대량 진열 |
| 유기·철물 | 소수의 그릇·기물 실루엣 | 고광택 금속 과장·상품 과밀 |
| 신발·생활 잡화 | 작은 쌍·상자 | 시대가 다른 고무신·현대 진열대 |

시대가 지정되지 않은 기본 장면에는 도입 시기가 분명히 늦은 담배 같은 물종을 대표 표식으로
강조하지 않는다. 작은 표식은 `시명/물종이 있었다`는 사실만 전달하며, 정확한 판 모양·서체·색·
부착 높이는 제품 결정으로 표시한다.

### 1.4 발굴 유구가 말하는 범위

서울역사박물관은 청진지구 발굴에서 화재와 전란으로 불탄 채 묻힌 기둥, 마룻장, 문짝과
생활 도구가 확인되었다고 설명한다. 박물관 보도자료와 야외전시 안내는 2004년 종로1가·청진동
일대에서 발견된 조선시대 시장 점포 터 일부를 이전·복원했다고 기록한다.

이 자료는 다음 표현을 지지한다.

- 기둥과 목재 수평재가 만드는 칸 리듬
- 바닥 높이와 후퇴한 실내의 얕은 깊이
- 완전히 비어 있는 가설 천막으로 환원할 수 없는 문·마루를 갖춘 목조건축
- 종로 전면, 피맛길, 배후 주거가 층을 이루는 도시 조직

그러나 공개 해설만으로 발굴된 각 문짝이 어느 점포의 어느 전면에 달렸는지, 모든 칸이 같은
방식으로 열렸는지, 완전한 입면과 지붕 결구가 어떠했는지는 복원할 수 없다. 발굴 유구는
실제 건축의 존재와 재료·도시 맥락을 확인하는 근거이지, 임의의 완전 입면을 실측 복원으로
격상시키는 근거가 아니다.

### 1.5 도가와 판매 행랑은 같은 것이 아니다

「도가와 시전 행랑」은 도가를 시전 도중의 사무실·회의 장소이자 창고로, 상품 거래가
이루어지는 시전행랑과 구분한다. 주요 도가의 층수와 규모를 짐작하게 하는 단편 기록은 있지만
구체적인 모습은 거의 남지 않았다고 출처 자체가 한계를 밝힌다.

이번 범위는 도가를 모든 점포 뒤에 반복하지 않는다. 얕은 후면 저장 공간은 판매 행랑의
깊이를 읽게 하는 제품 표현일 뿐, 각 칸을 독립 도가로 재현한 것이 아니다.

## 2. 확인되지 않은 입면과 절제 원칙

다음은 현재 선별 출처가 확정하지 않는다.

- 모든 점포 전면의 개방 비율과 여닫는 방식
- 판매대·상·문턱의 보편 형식, 치수와 위치
- 칸의 보편 실측 폭·깊이·기둥 높이
- 표의 정확한 재료·형태·서체·색·부착 위치
- 지붕이 몇 칸마다 끊겼는지와 모든 구간의 동일한 지붕 형식
- 백색 회벽이 모든 시대·시전에 공통이었다는 주장
- 한 시점에 진열된 상품의 양과 점포별 분포

특히 우리역사넷이 설명하는 `상전(床廛)`은 물건을 상 위에 놓아 판 특정 시전 어휘다. 이를
모든 시전 전면에 동일한 고정 카운터가 있었다는 증거로 일반화하지 않는다. cheoma가 두는
낮은 bench는 두 칸의 깊이를 반복해서 읽히게 하는 **제품 해석**이며, 보편적인 역사 형식이나
실측 치수가 아니다. 계획 footprint 안에 머물고 보행 회랑으로 내밀지 않는다.

또한 조선 전기 조성과 후기의 화재·재건·임대 기록, 여러 시기의 발굴 층위를 한 날짜의 모습으로
합치지 않는다. 현재 장면은 시전행랑이라는 장기 지속 공간 문법을 표현하는 것이며 특정 연도의
종로 복원도가 아니다.

## 3. cheoma의 제품 번역

### 3.1 순수 계획이 소유할 것

`src/village/sijeon-plan.js`는 Three.js와 전역 `Math.random`에 의존하지 않고, 기존
`shop.poly`, `center`, `frontDir`, `w`, `d`를 보존한 채 칸별 façade 의미만 파생한다.
renderer가 shop ID나 역할 이름으로 두 번째 배치를 추론해서는 안 된다.

계획은 `planSijeon(roadsResult, site, char01, { reach, runCap })`로 기존 배치 record를 만들고,
`planSijeonFacade(shop)`으로 각 점포의 로컬 façade를 파생한다. `reach(pt)`는 그 지점에 행랑을
이어붙일 수 있는가(기본은 분지 반경 컷, 성곽 도성은 "성벽 안쪽 + 문전 마당 밖")이고, `runCap`은
(도로, 면)당 **점포** 상한이다. 상한은 프리픽스 컷이므로 유한값에서는 도로 시작부만 채워진다 —
성곽 도성 tier 는 `Infinity`를 넘겨 파사드 범위를 `reach`가 정하게 한다(2026-08-04 개정,
근거·실측은 `docs/joseon-city.md` §성문 주변). 예약 공백(`kind: 'break'`)은 상한을 소비하지 않는다. façade 좌표는 `+x`가 행랑
진행 방향, `+z`가 도로를 향한 전면이다. 현재 schema v3는 한 점포를 제품상 2칸으로 나누며
다음 물리 의미를 소유한다.

- placement의 안정적인 점포 ID와 façade 부재의 칸 index
- 세 전면 기둥, 두 상인방과 두 후퇴 개구
- 각 칸의 낮은 display bench
- 후면 저장 매스
- 맞배지붕과 전·후·측면 처마 내밀기
- 일반 부재의 도로 쪽 한계와 처마만 허용하는 명시적 예외
- (v2) 점포 ID 안정 hash로 소수 선택된 **장식용 표식 판** 0–1개 (`signs[]`)
- (v3) 벽체: 배면벽·측벽 2·전면 상벽(`walls[]`), 박공벽 프리즘 2(`gables[]`),
  그리고 벽 위~지붕면 사이의 높이 계약(`roofline`)

시명·물종 표식은 칸/점포 ID에서 파생된 순수 의미를 먼저 계획에 기록한다. renderer가 자체
난수나 배열 순서로 두 번째 변주를 만들지 않는다. 두 bench는 칸의 깊이를 읽히게 하는 제품
수치·형식이며, 모든 역사적 시전에 같은 고정 판매대가 있었다는 주장이 아니다.

**schema 경계**

| 버전 | 소유 | 의도적 비소유 |
| --- | --- | --- |
| v1 | 열주·상인방·후퇴 개구·판문·bench·후면 저장·맞배 | 표식·상품 |
| v2 | v1 + 소수 `marker-board` 장식 실루엣 | 읽을 수 있는 시명/물종 문자열, SKU, 상품 진열 카탈로그, 발광·네온 |
| v3 (`SIJEON_FACADE_SCHEMA_VERSION=3`) | v2 + 배면벽·측벽·박공벽·전면 상벽과 `roofline` 높이 계약 | 전면 개구를 덮는 벽, 벽 전용 재질·텍스처, 창·문 개구 어휘의 확장 |

v2 표식 plan 규칙 (`SIJEON_SIGN_POLICY`):

- **한양 only 경로**: 제품 배치가 한양(또는 강제 `sijeon=true` 간선)에서만 점포를 내므로 표식도 그
  범위에만 나타난다. 농촌 규모에 시전을 새로 퍼뜨리지 않는다.
- **소수**: 점포당 최대 1개, 선택 확률 상한 `maxShare=0.28` (제품 성김 — 역사 빈도 주장 아님).
- **비발광**: plan `emissive: false`. renderer는 frame/wood 재질만 빌려 쓰며 emissive·새 material
  family·텍스처·글자 glyph를 만들지 않는다.
- **장식 실루엣만**: `silhouette` ∈ {`tablet`, `plank`}. `name` / `label` / `commodity` / `text` /
  `sku` 필드를 갖지 않는다. “표가 있었다”는 사실만 전달하고 특정 시명·물종을 단정하지 않는다.
- **결정론**: `hashString('sijeon-sign|'+shop.id)`. 전역 `Math.random` 소비 0. `shop.id`가 없으면
  `signs=[]` (순수 치수 fixture는 표식 없음).
- **회랑**: 표식 판은 건물 전면(`frontZ`) 안쪽에 걸려 도로 회랑(`maxNonEaveZ`)을 넘지 않는다.

상품(진열 실루엣) record는 v2에도 넣지 않는다. bench는 비어 있어도 유효하며, 상품을 임의로
채워서는 안 된다. 후속 상품 schema가 필요하면 같은 방식으로 순수 plan 필드를 먼저 추가한다.

구조 부재(기둥·개구·bench·저장·지붕)와 점포 위치·footprint는 id 유무와 표식 선택에 불변이다.
장식 때문에 도로·성곽·필지·worker 배치 바이트가 달라져서는 안 된다.

### 3.3 행랑 줄 분절 (#218, 범위 a)

**이번 PR 범위:** 긴 간선 façade의 **줄 분절/끊김**만 다룬다. 끝단 맞배 박공(b)과 피맛길
암시(c)는 비범위 잔여다. 표식·상품은 #227.

**제품 해석 (사료 과대주장 금지):**

- 출처는 간선에 평행한 긴 시전행랑·칸 단위 사용·청진동 일대의 도시 조직을 확인한다. **지붕이
  몇 칸마다 끊겼는지**, 모든 구간이 동일 형식인지는 확정하지 않는다(`§2`).
- 따라서 235 m급 무단절 단일 실루엣은 출처가 지지하는 복원이 아니라, 배치 pitch와 측면 처마가
  겹쳐 생긴 **제품 극단**이었다.
- cheoma는 이를 역사 실측 복원으로 고치지 않고, 다음 **제품 규칙**으로 읽히게 한다.

| 상수 | 값 | 의미 |
| --- | --- | --- |
| `segmentShops` | 5 | 한 블록의 연속 점포 수(점포당 제품 2칸 → 블록당 10칸 상당). 실측 칸 수가 아니다. |
| `segmentGapPitches` | 1 | 블록 사이 빈 footprint 1 pitch(6.2 m). 서비스/방화 틈·소유 블록 경계의 **암시**이며 피맛길이 아니다. |
| `kind: 'break'` | — | 렌더 mass 없음. `poly`는 필지 blocker로 남아 주거가 시전 회랑을 채우지 않는다. |
| `segment.{id,index,length,role}` | start/mid/end/solo | 후속 끝단 처리(b)가 소비할 순수 메타. 전역 RNG 0. |

점포 본체 폭은 pitch×0.96, 지붕 폭은 본체+1.4 m라 인접 지붕이 겹친다. 한 pitch 공백을 두면
상점 중심 간 거리는 2 pitch≈12.4 m이고 측면 처마 사이 틈은 약 5 m — 부감·근경에서 연속 복붙
지붕이 아니라 **블록 단위 행랑**으로 읽힌다. 교차 간선 회피·분지 밖으로 생기는 자연 끊김은
그대로 두고, 그 뒤에는 새 블록을 시작한다.

**피맛길이 아닌 이유:** 피맛길은 시전 **배후**에서 간선과 평행한 이면 골목이다. 이번 끊김은
행랑 **열 방향**의 분절이며 배후 골목을 그리지 않는다.

**draw / program 예산 (불변 목표):**

| 항목 | 예산 | #218 후 |
| --- | --- | --- |
| 병합 루트 | 1 `village-sijeon` | 동일 |
| draw call / mesh | ≈5 (역할 병합) | 동일 — break는 geometry 0 |
| material | 5 (frame/opening/bench/storage/roof) | 동일 |
| 신규 텍스처 | 0 (공유 `tileTex` clone만) | 동일 |
| program family | 점포 수에 비례 금지 | 유지 |
| 전역 RNG | 0 | 유지 |

검증: `npm run check:sijeon` (순수 배치·분절·facade), 의도된 Hanyang plan/worker 해시 재기준,
`shoot:sijeon` 드로우 예산.

### 3.4 벽체 완결 (#54, schema v3)

**문제(실측 2026-08-04):** 행랑이 사대문 접근로까지 확장된 뒤(커버리지 22.1%→88~96%) 점포가
정면이 아닌 각도로 잡히기 시작했는데, v2 계획은 **전면 골조·후면 저장 박스·박공지붕만** 소유했다.
가장 높은 부재가 `y=2.78`이고 지붕 처마 밑면이 `y=BODY_HEIGHT=3`이라 지붕판이 벽 없이 떠 보이고
밑면이 그대로 노출됐다(남문·서문 망원, 사부감). 배치는 합법이었고 결함은 **파사드 어휘**였다.

**계획이 소유하는 것(순수·결정론, 전역 RNG 0):**

| 부재 | 범위 | 근거·규칙 |
| --- | --- | --- |
| `rear-wall` | 뒷변 전폭 × 지면~`roofline.closureTopY` | 배면을 닫는다. `rear-storage`는 유지하되 이 벽 안쪽으로 물러난다. |
| `side-wall` ×2 | 측면 전깊이 × 지면~본체 높이 | 두께 = `halfWidth − (bayWidth + openingWidth)/2` = 0.775×기둥폭(6.2 m 점포에서 0.20 m). **개구가 정한 상한**이며 미감 선택이 아니다. |
| `gable-wall` ×2 | 본체 높이~지붕면, 로컬 `(z, y)` 볼록 프로파일 | 측벽 위 맞배 단면을 채워 옆·사선에서 지붕 밑면을 없앤다. renderer가 두께만큼 x로 압출한다. |
| `front-header` | 인방 상단~`closureTopY`, 전폭 | 포벽 자리의 얇은 상벽. 기둥·인방·개구 아래는 그대로 열려 있다. |
| `roofline` | `wallTopY` / `closureTopY` / `ridgeY` / `slabAllowance` | 벽 위~지붕면 높이 계약. closure 부재는 본체 높이 상한이 아니라 이 상한을 쓴다. |

`closureTopY = max(본체높이, 벽 평면의 지붕면 − slabAllowance)`. `slabAllowance = 0.12 m`는
renderer의 지붕판 두께(0.14 → 최대 수직 여유 0.079 m)를 덮는 **부재 관통 방지용 구조 여유**다.
실측: 박공 프로파일이 건물 깊이 전 구간에서 지붕면보다 정확히 0.12 m 아래에 머문다.

**개방 전면은 축소하지 않는다.** 기둥 3·인방 2·판문 2·좌판 2는 발굴 사료가 확인한 어휘이므로
(§1.4, §3.2-4) 벽이 계획된 개구와 겹치면 `check:sijeon`이 실패한다.

**예산 델타(순수 노드 실측, hanyang/20260716 197점포):**

| 항목 | 전 | 후 |
| --- | --- | --- |
| 병합 메시 / 재질 / 텍스처 | 5 / 5 / 0 | 5 / 5 / 0 (불변) |
| 시전 삼각형 | 43,740 | 59,500 (+15,760 = 점포당 +80) |
| program family | 점포 수 비종속 | 동일 (`shoot:sijeon` programs 4 불변) |

새 재질·텍스처·역할이 0인 이유: 벽 mass는 이미 벽 의미로 쓰이던 `storage` 역할 재질을 빌려 쓴다.
박공벽만 프리즘이라 `unitBox` 대신 압출 geometry를 쓰지만 같은 재질 버킷으로 병합된다.

### 3.2 renderer가 표현할 것

전용 시전 renderer는 계획을 그대로 읽어 다음 순서로 깊이를 만든다.

1. 계획이 준 벽체(배면·측면·박공·전면 상벽)와 후면 저장 매스로 연속 행랑의 큰 실루엣을 만들고,
   지붕을 그 질량 위에 앉힌다. 벽은 새 역할을 만들지 않고 `storage` 재질을 빌려 쓴다(v3, §3.4).
2. 전면 벽을 처마 안쪽으로 후퇴시킨다.
3. 세 전면 기둥과 두 상인방으로 제품상의 두 칸을 다시 읽힌다.
4. 두 개구를 기둥선 뒤로 물리고 판문·목재 면과 깊은 음영을 절제해 빈 동굴이나 현대
   쇼윈도를 피한다.
5. 낮은 display bench는 비운 채로도 칸의 전후 깊이를 설명한다.
6. (v2) plan이 준 `signs[]`만 읽어 작은 목재 표식 판을 **기존 `frame` 재질**로 박스 병합한다.
   새 material·텍스처·프로그램·드로우 패밀리를 만들지 않는다.

현대 유리, 네온·emissive 상호, NPC 군중, 천막 숲, 모든 칸의 상품 과밀은 넣지 않는다. 가까운
화면의 생활감은 우선 기둥 뒤의 깊이·그림자와 빈 bench, 그리고 소수 표식 실루엣으로 만들고,
원경의 정보량은 긴 처마선과 칸 리듬이 맡는다.

**plan / render 경계 (v2 표식)**

| 층 | 소유 | 금지 |
| --- | --- | --- |
| pure plan (`sijeon-plan.js`) | 존재 여부·bay·silhouette·center/size·`decorative`/`emissive:false`, (v3) 벽체 box·박공 프로파일·`roofline` | Three, Math.random, 시명 문자열, 물종 enum 단정 |
| renderer (`generators/village/sijeon.js`) | plan box → borrowed `materials.frame` mesh, (v3) 벽체·박공 → borrowed `materials.storage` mesh, 정적 병합 | 자체 난수로 표식 추가, emissive, 새 material family, 텍스처 글자, 벽 위치·박공 형상 재추론 |
| 제품 어댑터 (`features.js`) | 기존 5역할 palette (frame/opening/bench/storage/roof) | 표식 전용 재질 clone |

상품 진열 어휘는 역사적 근거로 문서와 References UI에는 남기지만 **rendered v2에서도
제외**한다. 표식만 장식 실루엣으로 허용한다.

## 4. 성능·표현 경계

### 4.1 병합과 자원

- 점포 수와 무관하게 역할별 geometry를 모아 하나의 `village-sijeon` 정적 병합 그룹으로
  유지한다. 목표는 새 텍스처 0, 약 5 draw call이며 칸마다 Mesh·Material을 만들지 않는다.
- 개구·bench·v2 표식 판은 공유 palette와 병합된 geometry로 표현한다. 표식은 `frame` 역할에
  합쳐지므로 점포 수와 무관하게 약 5 draw call 목표가 유지된다. 후속 물종도 새 material
  variant를 만들지 않고 병합 geometry 차이만 사용할 수 있다.
- 계획은 숫자·문자열·작은 배열만 반환하고 Three 객체나 dispose 책임을 갖지 않는다.
- renderer가 만든 geometry와 palette clone은 그룹 수명에 맞춰 한 번 해제되며, 호출자 공유
  재질의 소유권을 빼앗지 않는다.

### 4.2 거리와 LOD

- **원경**: 지붕과 연속 가로벽, 큰 칸 간격만 읽혀야 한다. 작은 상품·표식의 색이나 글자를
  원경 가독성 조건으로 삼지 않는다.
- **중경**: 열주, 상인방, 후퇴한 전면의 명암이 흰 상자 실루엣을 깨야 한다.
- **근경**: 기둥선 뒤의 두 개구, 낮은 bench와 후면 저장 매스가 깊이를 설명해야 한다.

별도 FAR impostor나 점포별 `THREE.LOD`를 추가해 두 표현을 동시에 유지하지 않는다. 작은 detail을
거리에서 숨길 필요가 있다면 이미 병합된 의미 묶음을 안정적인 그룹 단위로 제어하고, 화면 크기에
비해 보이지 않는 요소를 처음부터 절제하는 것을 우선한다. 거리 전환 때문에 draw call, material
program 또는 텍스처가 점포 수에 비례해서는 안 된다.

### 4.3 눈·림라이트

- 기둥·처마·후퇴선은 색에 기대지 않고 실루엣과 명암만으로 읽혀야 하며, 표현 전용 mesh나
  material을 따로 만들지 않는다.
- 기와 지붕만 기존 roof 역할과 `snowSurface` 계약을 통해 눈을 받는다. 수직 열주, 열린 전면,
  bench, 후면 저장 면과 v3 벽체·박공벽에 지붕용 적설을 누출하지 않는다(벽체는 `storage` 재질을
  빌려 쓰므로 `snowSurface` 태그가 붙은 roof 재질과 분리된다). 새 눈 텍스처나 눈 geometry를
  추가하지 않는다.
- 석양 림은 실제 sun-facing silhouette와 shadowed main-light 조건을 통과한 기존 물리 rim
  patch만 받으며, 어두운 처마 아래를 임의로 밝히지 않는다. 미래의 표식·상품도 발광하지 않는다.

### 4.4 리롤 wave

`village-sijeon`은 기존 wave 분류의 **그룹 단위** 구조물이다. 구·신 시전을 투명 재질로
crossfade하지 않고, 먹안개 handoff 아래에서 배타적으로 소유권을 넘긴다. 그룹의 제 바운딩 하단을
pivot으로 기존 tofu scale·drop을 적용하며 다음을 금지한다.

- `transparent`, `opacity`, `depthWrite`를 전환용으로 변경
- 구·신 행랑의 동시 가시와 겹친 그림자
- wave 동안 임시 material/program 생성
- 완료·취소 후 변형 scale, visibility, geometry 소유권 잔류

## 5. 완료 체크리스트

### 역사성과 해석

- [x] 긴 간선변 행랑, 칸 단위 사용, 시명·물종 표식(장식 실루엣만), 발굴 유구의 범위를 구현 설명과
      Reference UI에서 확인할 수 있다.
- [x] 정확한 개방 전면·판매대·칸 치수·표식 모양은 제품 결정임을 명시한다.
- [ ] 도가를 모든 판매 칸의 후면으로 오해하지 않는다.
- [ ] 특정 연도의 종로 또는 청진동 발굴지 실측 복원이라고 표시하지 않는다.

### 계획과 배치

- [ ] 기존 pitch·depth·setback·runCap 수치는 유지하고, #218 분절은 같은 sample 열 위의
      `kind`/`segment` 메타와 예약 break만 추가한다(break poly는 필지 blocker).
- [ ] 전역 `Math.random` 소비가 0이고 동일 입력의 plan과 geometry 결과가 동일하다.
- [ ] 연속 shop 런 ≤ `segmentShops`, break 양옆 지붕이 맞닿지 않는다(`check:sijeon`).
- [ ] 열주·bench·후면 저장 매스와 처마의 명시적 예외가 도로 회랑과 인접 footprint 계약을
      지킨다.
- [ ] renderer가 칸·물종·개방 상태를 다시 추론하지 않고 순수 plan을 소비하며 break mass를
      그리지 않는다.

### 시각과 성능

- [ ] 낮·석양에서 거리 아이레벨, 사선 부감, 한양 전체 부감을 직접 비교한다.
- [ ] 원경은 연속 처마선, 중경은 칸 리듬, 근경은 후퇴 깊이와 절제된 생활감이 읽힌다.
- [x] 어느 각도에서도 지붕이 벽체 질량 위에 앉는다 — 배면·측면·박공·전면 상벽이 닫히고 계획된
      개구는 그대로 열려 있다(`check:sijeon` 신규 단언, §3.4).
- [x] 새 텍스처 0, 점포 수와 무관한 단일 병합 그룹, 약 5 draw call 목표를 지킨다(v3 후 실측 5/5/0).
- [ ] snow·물리 rim·wave와 dispose 계약을 자동 검사한다.
- [ ] worker scene hash 변화는 의도한 Hanyang 시전 geometry에 한정되고 다른 규모와 picking
      계약은 유지된다.

## 6. 선별 출처·접근·권리

아래 URL은 2026-07-24에 로그인 없이 본문 접근을 확인했다.

1. [국사편찬위원회 우리역사넷, 고동환, 「조선 전기 신도의 시전 상업」](https://contents.history.go.kr/mobile/km/view.do?levelId=km_003_0040_0020_0010)
2. [국사편찬위원회 우리역사넷, 고동환, 「도가와 시전 행랑」](https://contents.history.go.kr/front/km/view.do?levelId=km_003_0040_0040_0050_0010)
3. [국사편찬위원회 우리역사넷, 「유분각전과 무분각전」](https://contents.history.go.kr/front/km/view.do?levelId=km_003_0040_0030_0020)
4. [국사편찬위원회 우리역사넷, 한국사 연대기 「금난전권」](https://contents.history.go.kr/mobile/kc/view.do?code=kc_age_30&levelId=kc_i301500)
5. [서울역사박물관, 「청진동 발굴 유물」(유물번호 318)](https://museum.seoul.go.kr/www/exh/NR_exhibitInfoView.do?arcvGroupNo=2177&arcvMetaSeq=20513&arcvNo=63625&langGubun=01&sso=ok)
6. [서울역사박물관, 「종로 시전행랑 유구 이전복원 야외 전시」, 2009-09-15](https://museum.seoul.go.kr/www/board/NR_boardView.do?bbsCd=1015&seq=00000000000000017&sso=ok)

국사편찬위원회 저작권 정책은 위원회가 권리 전부를 갖지 않은 자료가 함께 있으며 출처·저자·URL을
구체적으로 표시하고 개별 이용조건을 확인하라고 안내한다.

- 저작권 정책: https://www.history.go.kr/contents/contentsPage.do?groupId=000000000571&menuId=000000000572&pageId=000000000213

서울역사박물관의 위 두 상세 페이지에는 별도의 항목별 공공누리 유형이 표시되지 않고 사이트 하단에
`ALL RIGHTS RESERVED`가 표시된다. 따라서 cheoma는 두 기관 자료 모두에서 링크와 독자적인 사실 요약만
제공하고, 원문 문장·사진·발굴 도판·이전복원 형상을 복제하지 않는다. 이미지나 보고서를 별도 자산으로
재사용하려면 해당 기관의 최신 권리 표시와 허락 범위를 다시 확인한다.
